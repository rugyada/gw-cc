#!/bin/bash
	enable-repo restricted
konsole -e sh -c "pkexec dnf install lib64dvdcss2 faac flac x264 lib64xvid4 lib64dca0 x265 qt-heif-image-plugin heifthumbnailer; echo 'Press enter to close konsole'; read"
